---
name: Column
route: /Column_Test
menu: Visualisations Test
---

<h1 style={{ margin: '0px' }}>Column Chart</h1>

<br />

## Import

```javascript
  import { Column } from 'motor-ui'
```

<br />

## Usage


import { Props } from 'docz'
import { Playground } from 'docz'
import Column from '../../lib/components/React/Column/Column.jsx';
import Bar from '../../lib/components/React/Bar/Bar.jsx';
import Motor from '../../lib/components/React/Motor/Motor.jsx';
import { config } from '../config/config.js'

<Playground>
<Motor  
  config={{
            host: "localhost",
            secure: false,
            port: 19077,
            prefix: "",
            appId: "5c8ddda7-d4a8-4ee1-b994-922fca34830c"
  }}>

            <Column
            showAxis='none'
            cols={[
           //   { qField: '[OICA region]', qLabel: 'OICA region' },
              { qField: 'Territory', qLabel: 'Territory' },
              { qField: '=Sum([Car sales])', qLabel: 'Car sales',
       //            { qField: '=Sum([Commercial vehicle sales])/Sum([Car sales])', qLabel: 'Car sales',
       //            qNumFormat: {
       //       "qType": "R",
       //       "qnDec": 14,
       //       "qUseThou": 1,
       //     //  "qFmt": "##############",
       //       "qFmt": '#,##0.00%',
       //       "qDec": ".",
       //       "qThou": ","
       //     }, 
       }
             //       { qField: '=Sum([Commercial vehicle sales])/Sum([Car sales])', qLabel: 'Car sales Ratio',
          //  }
            ]}
             title="Vehicle Sales by Region"
            showGridlines={true}
           //  suppressScroll={true}
            // textOnAxis='none'
            // showLegend={false}
            // suppressZero={true}
            // stacked={true}
            // percentStacked={true}
            height="410px"
           // tickSpacing="wide"
            // border = {false}
            // backgroundColor="none"
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}/>

           
     
  </Motor>
</Playground>

<br />

<Playground>

<Motor  
  config={{
            host: "localhost",
            secure: false,
            port: 19077,
            prefix: "",
            appId: "5c8ddda7-d4a8-4ee1-b994-922fca34830c"
  }}>

                     <Column
            cols={[
              { qField: '[OICA region]', qLabel: 'OICA region' },
            //  { qField: '=Sum([Car sales])', qLabel: 'Car sales' }
          //    { qField: '=Sum([Commercial vehicle sales])/Sum([Car sales])', qLabel: 'Car sales Ratio',
          //    { qField: '=Sum([Car sales]*if([OICA region]="Americas",-1,1))', qLabel: 'Car sales Ratio',

             { qField: '=Sum([Car sales])*(1)', qLabel: 'Neg Car sales' }
        //           qNumFormat: {
        //      "qType": "R",
        //      "qnDec": 14,
        //      "qUseThou": 1,
        //    //  "qFmt": "##############",
        //      "qFmt": '#,##0.00%',
        //      "qDec": ".",
        //      "qThou": ","
        //    }, 
        //   }
            ]}
           //  title="Vehicle Sales by Region"
           //  subTitle="Vehicle Sales by Region"
            showGridlines={true}
            showLabels="inside"
            // suppressScroll={true}
            // textOnAxis='none'
            // showLegend={false}
            // suppressZero={true}
            // stacked={true}
            // percentStacked={true}
            height="410px"
            //tickSpacing="narrow"
            // border = {false}
            // backgroundColor="none"
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}/>

           
     
  </Motor>
</Playground>

<br />

<Playground>

<Motor  
  config={{
            host: "localhost",
            secure: false,
            port: 19077,
            prefix: "",
            appId: "5c8ddda7-d4a8-4ee1-b994-922fca34830c"
  }}>

                     <Column
            cols={[
              { qField: '[OICA region]', qLabel: 'OICA region' },
              { qField: '=Sum([Car sales])', qLabel: 'Car sales' },
              { qField: '=Sum([Commercial vehicle sales])', qLabel: 'Commercial vehicle sales' }
            ]}
            // title="Vehicle Sales by Region"
            showGridlines={true}
             showLabels="inside"
            // suppressScroll={true}
            // textOnAxis='none'
            // showLegend={false}
            // suppressZero={true}
            // stacked={true}
            // percentStacked={true}
            height="410px"
            //tickSpacing="narrow"
            // border = {false}
            // backgroundColor="none"
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}/>

           
     
  </Motor>
</Playground>

<br />

<Playground>

<Motor  
 config={config}>  
          <Column
            cols={[
              { qField: 'country', qLabel: 'Country' },
            //  { qField: '=Count(winery)', qLabel: 'No of Wineries' },
                    { qField: '=Avg(price)*if(Country="Austria",-1,1)', qLabel: 'Average Price' }
            ]}
            // title="Vehicle Sales by Region"
            showGridlines={true}
            tickSpacing="wide"
            // suppressScroll={true}
            // columnPadding={5}
            // zoomScrollOnColumnWidth={10} //to add?
            // textOnAxis='none'
            // showLegend={false}
            //suppressZero={true}
            //stacked={true}
            // percentStacked={true}
            height="410px"
            // border = {false}
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}/>
  </Motor>
</Playground>

<br />

<Playground>
<Motor  
 config={config}>  
          <Column
            cols={[
           //   { qField: 'country', qLabel: 'Country' },
           //  { qField: 'variety', qLabel: 'Province' },
           //   { qField: '=Count(distinct winery)', qLabel: 'No of Wineries' },
                         { qField: 'country', qLabel: 'Country' },
             { qField: 'province', qLabel: 'Province' },
              { qField: '=Avg(price)', qLabel: 'Average Price' }
            ]}
            // title="Stacked Column Chart"
            showGridlines={true}
             tickSpacing="wide"
            // suppressScroll={true}
            // columnPadding={5}
            // zoomScrollOnColumnWidth={10} 
            // textOnAxis='none'
            // showLegend={false}
            //suppressZero={true}
            stacked={true}
          //   percentStacked={true}
            height="410px"
            // border = {false}
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}/>
  </Motor>
</Playground>

<br />


## Props
