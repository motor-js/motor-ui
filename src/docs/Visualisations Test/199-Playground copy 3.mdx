---
name: Bar Brushing
route: /barchart_test_brushing
menu: Visualisations Test
---

<h1 style={{ margin: '0px' }}>Bar Chart</h1>

## Usage


import { Props } from 'docz'
import { Playground } from 'docz'
import Bar from '../../lib/components/React/Bar/Bar.jsx';
import Column from '../../lib/components/React/Column/Column.jsx';
import Motor from '../../lib/components/React/Motor/Motor.jsx';
import { config } from '../config/config.js'

<Playground>
  <Motor  
  config={{
            host: "localhost",
            secure: false,
            port: 19077,
            prefix: "",
            appId: "5c8ddda7-d4a8-4ee1-b994-922fca34830c"
  }}>
      <Bar
            cols={[
            //  { qField: 'Year', qLabel: 'Year' },
              { qField: 'Territory', qLabel: 'Territory' },
              { qField: '=Sum([Car sales])', qLabel: 'Car sales' }
            ]}
            // title="Vehicle Sales by Region"
            // subTitle="Vehicle Sales by Region"
                        tickSpacing="narrow"
                        showLabels="inside"
            showGridlines={true}
            textOnAxis='both'
            // showLegend={true}
            suppressZero={true}
           // suppressScroll={true}
        //   scrollRatio= {0.5}
            stacked={true}
            // percentStacked={true}
            height="410px"
            // border = {false}
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}></Bar>
     
  </Motor>
    <Motor  
  config={{
            host: "localhost",
            secure: false,
            port: 19077,
            prefix: "",
            appId: "5c8ddda7-d4a8-4ee1-b994-922fca34830c"
  }}>
      <Column
            cols={[
            //  { qField: 'Year', qLabel: 'Year' },
              { qField: 'Territory', qLabel: 'Territory' },
              { qField: '=Sum([Car sales])', qLabel: 'Car sales' }
            ]}
            // title="Vehicle Sales by Region"
            // subTitle="Vehicle Sales by Region"
                        tickSpacing="narrow"
            showGridlines={true}
            textOnAxis='both'
            // showLegend={true}
            suppressZero={true}
           // suppressScroll={true}
            stacked={true}
            // percentStacked={true}
            height="410px"
            // border = {false}
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}></Column>
     
  </Motor>

</Playground>

<br />

