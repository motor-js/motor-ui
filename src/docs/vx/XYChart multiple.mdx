---
name: XYChart multiple
route: /xychartMultiple
menu: visx
---

<h1 style={{ margin: '0px' }}>XYChart</h1>

<br />

## Usage

Here is a bar chart.
Check out the props and example usage below 💡

import { Props } from 'docz'
import { Playground } from 'docz'
import XYChart from '../../lib/components/XYChart/XYChart.jsx'
import Combo from '../../lib/components/Combo/Combo.jsx'
import Line from '../../lib/components/Line/Line.jsx'
import Bar from '../../lib/components/Bar/Bar.jsx'
import Motor from '../../lib/components/Motor/Motor.jsx'
import Filter from '../../lib/components/Filter/Filter.jsx'
import Button from '../../lib/components/Button/Button.jsx'
import { config } from '../config/config.js'

<Playground>
<Motor  
   config={{
  host: "juno-ui.eu.qlikcloud.com",
  secure: true,
  port: null,
  prefix: "",
  // appId: "e6b8cc66-acce-4fd0-9cff-26ea2e987cde",
  appId: "39e218c1-42ee-4f38-9451-1b8f850505d5",
  // appId: "0c07cd52-e6a1-45ff-a605-fd37394f90bf",
  qcs: true,
  webIntId: "4Tx-ydWxSQEM_q1ajlYBVzGgVUVJUo-i",
  }}>  
  <Filter label='Year' dimension={['Year']} />
  <Filter label='OICA region' dimension={['OICA region']} />
      <Button type="clearSelections">Clear Selections</Button>
     <Combo
        width={750}
        height={400}
        cols={[
          { qField: "[Month]", qLabel: "Month" },
          {
            qField: "=Sum([Sales Margin Amount])",
            qLabel: "Margin",
            qChartType:'bar',
            qCondChartColor: '=if(Sum([Sales Margin Amount])>4000000, red(), blue())',
            qNumFmt: "£#,##0",
            qNumType:"R"
          },
          {
            qField: "=Sum([Sales Amount])",
            qLabel: "Sales",
            qChartType:'line',
            qShowPoints: { symbol: 'square'},
            qNumFmt: "£#,##0",
            qNumType:"M"
          },
        ]}
      />
       <Bar
          width={800}
          height={350}
          colorTheme='divergent13'
          showLegend={false}
          showLabels={false}
          numMeasureTicks={6}
          stacked
          cols={[
            { qField: '[Month]', qLabel: 'Month' },
            { qField: '[Product Group Desc]', qLabel: 'Product Group'},
            { qField: '=sum([Sales Amount])', qLabel: 'Sales'},
          ]}
      />
  </Motor>
</Playground>
