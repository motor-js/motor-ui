---
name: XYChart multiple
route: /xychartMultiple
menu: visx
---

<h1 style={{ margin: '0px' }}>XYChart</h1>

<br />

## Usage

Here is a bar chart.
Check out the props and example usage below 💡

import { Props } from 'docz'
import { Playground } from 'docz'
import XYChart from '../../lib/components/React/XYChart/XYChart.jsx'
import Motor from '../../lib/components/React/Motor/Motor.jsx'
import Filter from '../../lib/components/React/Filter/Filter.jsx'
import Button from '../../lib/components/React/Button/Button.jsx'
import { config } from '../config/config.js'

<Playground>
<Motor  
   config={{
    host: 'localhost',
    secure: false,
    port: 19077,
    prefix: '',
    appId: '62160fbb-1fcc-4852-9edb-42dd048f6a3e',
  }}>  
  <Filter label='Year' dimension={['Year']} />
  <Filter label='OICA region' dimension={['OICA region']} />
      <Button type="clearSelections">Clear Selections</Button>
    <XYChart // width={750} height={400}  events={true}
    title="test"
    subTitle="=test"
    colorTheme="bio"
    selectionMethod="click"
    showClosestItem={false}
    // useSingleColor
   type="groupedbar"
    cols={[
      { qField: '[Year]', qLabel: 'Year' },
{ qField: '[OICA region]', qLabel: 'OICA region' },
      { qField: '=Sum([Car sales])*if(Year=2011,-1,1)', qLabel: 'Car sales' },
 //    { qField: '=Sum([Car sales])*if(Year=2011,1,1)', qLabel: 'Car ales' },
    ]}/>    <XYChart // width={750} height={400}  events={true}
    title="test"
    subTitle="=test"
    colorTheme="bio"
    selectionMethod="click"
    showClosestItem={false}
    // useSingleColor
   type="groupedbar"
    cols={[
      { qField: '[Year]', qLabel: 'Year' },
{ qField: '[OICA region]', qLabel: 'OICA region' },
      { qField: '=Sum([Car sales])*if(Year=2011,-1,1)', qLabel: 'Car sales' },
 //    { qField: '=Sum([Car sales])*if(Year=2011,1,1)', qLabel: 'Car ales' },
    ]}/>
  </Motor>
</Playground>
