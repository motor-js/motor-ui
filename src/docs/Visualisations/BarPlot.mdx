---
name: BarPlot
route: /barplot
menu: Visualisations
---

<h1 style={{ margin: '0px' }}>BarPlot Chart</h1>
<h3 style={{ margin: '0px' }}><em>A barplot chart</em></h3>

<br />

## Import

```javascript
  import { BarPlot } from 'motor-ui'
```

<br />

## Usage

import { Props } from 'docz'
import { Playground } from 'docz'
import BarPlot from '../../lib/components/React/BarPlot/BarPlot.jsx';
import Motor from '../../lib/components/React/Motor/Motor.jsx';
import { config } from '../config/config.js'

<Playground>
<Motor  
 config={config}>  
          <BarPlot
            cols={[
              { qField: 'country', qLabel: 'Country' },
              { qField: '=Count(winery)', qLabel: 'No of Wineries' }
            ]}
            // title="Vehicle Sales by Region"
            showGridlines={true}
            stacked={true}
            // percentStacked={true}
            height="410px"
            // border = {false}
            calcCondition={{
              qCond: '1+1=2',
              qMsg: 'Calculation condition has not been met'
            }}>
    </BarPlot>
  </Motor>
</Playground>

<br />

## Props

<Props of={BarPlot} />

<br />

## Theming


### Entry Point

```javascript
theme.barplot
```

### Object

Example below is taken from the Motor Theme.

```javascript

barplot: {
 /** Configuration object to connect to the Qlik Engine. Must include Qlik site URL and an App name */
  config: PropTypes.object,
  /** BarPlot label  */
  label: PropTypes.string,
  /** cols from Qlik Data Model to render in the BarPlot  */
  cols: PropTypes.array.isRequired,
  /** Calc condition for the chart  */
  calcCondition: PropTypes.object,
  /** Supress zeo vlaues in the the chart  */
  suppressZero: PropTypes.bool,
  /** BarPlot width */
  width: PropTypes.string,
  /** The height of the BarPlot */
  height: PropTypes.string,
  /** The amount of margin around the component */
  margin: PropTypes.string,
  /** Whether the KPI size should scale for tablet and mobile */
  responsive: PropTypes.bool,
  /** Size of the BarPlot */
  size: PropTypes.string,
  /** Color of the Labels on the BarPlot */
  fontColor: PropTypes.string,
  /** Border of the BarPlot Chart */
  chartBorder: PropTypes.string,
  /** Background Color of the chart */
  backgroundColor: PropTypes.string,
  /** RoundNum of the BarPlot */
  roundNum: PropTypes.bool,
  /** Title of the BarPlot */
  title: PropTypes.string,
  /** Sub Title of the BarPlot */
  subTitle: PropTypes.string,
  /** Legend of the BarPlot */
  showLegend: PropTypes.bool,
  /** Offset of the selction panel */
  offset: PropTypes.number,
  /** Display as Donut chart */
  innerRadius: PropTypes.number,
  /** Display as Donut chart */
  cornerRadius: PropTypes.number,
  /** Display as Donut chart */
  padAngle: PropTypes.number,
  },

```
