---
name: Motor
route: /juno
menu: Contexts
---

<h1 style={{ margin: '0px' }}>Motor</h1>
<h3 style={{ margin: '0px' }}><em>Parent component of the library, used for theming and to connect to the Qlik engine</em></h3>


import { Props } from 'docz'
import { Playground } from 'docz'
import { config } from '../config/config.js'
//import useCapabilityApi from '../../lib/hooks/useCapabilityApi.jsx'
import useEngine from '../../lib/hooks/useEngine.jsx'

<Playground>
  {() => {
    const { engine } = useEngine(config)
    
    return (
      <div>
      </div>
    );
  }}
</Playground>
