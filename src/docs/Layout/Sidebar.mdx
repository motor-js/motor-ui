---
name: Sidebar
route: /sidebar
menu: Layout
---

<h1 style={{ margin: '0px' }}>Sidebar</h1>
<h3 style={{ margin: '0px' }}><em>A Collapsable Sidebar</em></h3>

<br />


import { Props } from 'docz'
import { Playground } from 'docz'
import Sidebar from '../../lib/components/Sidebar/Sidebar.jsx'
import useSidebar from '../../lib/hooks/useSidebar.jsx'
import Motor from '../../lib/components/Motor/Motor.jsx'
import Button from '../../lib/components/Button/Button.jsx'
import Box from '../../lib/components/Box/Box.jsx'
import { config } from '../config/config.js'

<Playground>
  {() => {
  const { isOpen, toggle } = useSidebar();
      const handleClick = () =>{
    toggle()
    }
  return (
    <Motor config={config}>
      {/* <div style={{ overflow: "hidden", height: "300px", width: "500px" }}> */}
      <div style={{ overflow: "hidden", height: "300px"}}>
        <Sidebar
          width="30%"
          collapsable
          direction="column"
          padding="15px 12px"
          isOpen={isOpen}
          backgroundColor="altGray1"
          justifyContent="start"
          border={{ color: "brand" }}
          onOutsideClick={handleClick}
        >
          <Box direction="column" overflow="visible">
              <Button onClick={toggle} size={"small"}>
                Hide Sidebar
              </Button>
          </Box>
        </Sidebar>
        <Button onClick={toggle}>Show Sidebar</Button>
        <h1>Hello World</h1>
        <p>This is a test of what the sidebar would do if it had to work. Having a sidebar with an overlay.</p>
      </div>
    </Motor>
  );
  }}
</Playground>
