---
name: burger-menu
route: /burger-menu
menu: Layout
---

<h1 style={{ margin: '0px' }}>Sidebar</h1>
<h3 style={{ margin: '0px' }}><em>A Collapsable Sidebar</em></h3>

<br />


import { Props } from 'docz'
import { Playground } from 'docz'
import Motor from '../../lib/components/React/Motor/Motor.jsx'
import Button from '../../lib/components/React/Button/Button.jsx'
import Filter from '../../lib/components/React/Filter/Filter.jsx'
import NavItem from '../../lib/components/React/NavItem/NavItem.jsx'
import SmartHeading from '../../lib/components/React/SmartHeading/SmartHeading.jsx'
import SidebarNext from "../../lib/components/React/SidebarNext/SidebarNext.jsx";
import useSidebar from "../../lib/hooks/useSidebar";

import { config } from '../config/config.js'
import { BrowserRouter as Router, Route, Switch } from "react-router-dom";

<Playground>
  {() => {
    const { isOpen, toggle } = useSidebar();
    
    const handleClick = () =>{toggle()}
    return (
      <Router>
        <div id="anchor">
          <Motor config={config}>
          <div onClick={() => toggle()}>Open</div>
          <SidebarNext containerId={"anchor"} isOpen={isOpen} type="push"><div>Hello</div></SidebarNext>
          </Motor>
        </div>
      </Router>
    );
  }}
</Playground>
